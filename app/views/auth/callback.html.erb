<script>
(function () {
  // get data as a JSON encoded string
  var message = {
    name: <%= name.to_json.html_safe %>,
    data: <%= data.to_json.html_safe %>
  };

  var origin = location.origin;

  // send to opener
  if (window.opener) {
    window.opener.postMessage(message, origin);
  }
})();
</script>
